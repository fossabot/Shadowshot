<ResourceDictionary x:Uid="ResourceDictionary_1" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:tb="http://www.hardcodet.net/taskbar"
                    xmlns:viewModels="clr-namespace:Shadowshot.ViewModels">
    <tb:TaskbarIcon x:Uid="tb:TaskbarIcon_1" x:Key="NotifyIcon" IconSource="../Icon.ico" ToolTipText="Shadowshot">
        <tb:TaskbarIcon.DataContext>
            <viewModels:NotifyIconViewModel x:Uid="viewModels:NotifyIconViewModel_1" />
        </tb:TaskbarIcon.DataContext>
        <tb:TaskbarIcon.ContextMenu>
            <ContextMenu x:Uid="ContextMenu_1">
                <MenuItem x:Uid="MenuItem_1" Header="Settings" Command="{Binding SettingsCommand}"/>
                <Separator x:Uid="Separator_1" />
                <MenuItem x:Uid="MenuItem_2" Header="Exit" Command="{Binding ExitCommand}"/>
            </ContextMenu>
        </tb:TaskbarIcon.ContextMenu>
    </tb:TaskbarIcon>
</ResourceDictionary>